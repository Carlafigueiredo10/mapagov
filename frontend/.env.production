# Frontend - Produção (Render)
# Esta configuração será usada automaticamente quando rodar: npm run build

# URL da API Backend (usar URL relativa para mesmo domínio)
# DEIXE VAZIO para usar o mesmo domínio (https://mapagov.onrender.com)
VITE_API_URL=
VITE_PUBLIC_MVP_MODE=0
